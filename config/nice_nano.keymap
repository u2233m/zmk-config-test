#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer {
            // Даже если кнопок нет, нужно указать пустой биндинг
            bindings = <&none>;
        };
    };
};